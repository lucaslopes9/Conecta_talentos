# 🚀 Conecta Talent

Plataforma de recrutamento para o mercado de Angola.

## 📂 Estrutura do Projeto
- **/backend**: API em Node.js com MySQL.
- **/frontend**: Interface em Vue.js 3 com Vite.

## 🛠️ Como configurar
1. **Banco de Dados**: Importe as tabelas `users`, `empresas` e `vagas`.
2. **Backend**: Entre na pasta `/backend`, rode `npm install` e configure o `.env`.
3. **Frontend**: Entre na pasta `/frontend`, rode `npm install` e `npm run dev`.
